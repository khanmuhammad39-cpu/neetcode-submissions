class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        list_array = []

        for i in range(len(nums)):
            list_array.append(nums[i])
        

        for i in range(len(nums)):
            list_array.append(nums[i])

        return list_array
        